# Words that CONTAIN an axis word and must never be renamed.
#
# `controller` contains `roll`. About half the ~387 raw hits for the axis words
# in this repo are this one false positive, which is why tools/rename_axes.py
# matches whole IDENTIFIERS with non-word guards and never does a substring
# pass. This file is the record of what that guard protects, so the check can be
# repeated:
#
#   git grep -nE '\broll\b|\bpitch\b|\byaw\b|\bdelta[12]\b|\baxial\b'
#
# should return only hits containing one of these.

controller
Controller
ControllerNode
controllers
controllable
uncontrollable
JointPositionController
gz-sim-joint-position-controller-system
ros-jazzy-ros2-controllers
microcontroller
controlled
scrollbar
yscrollcommand
scrollregion
yview_scroll
rollingMomentCoefficient
rolling_moment_coefficient
coaxial
Coaxial
